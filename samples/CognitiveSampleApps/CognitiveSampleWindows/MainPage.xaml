<Page
    x:Class="CognitiveSampleWindows.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CognitiveSampleWindows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
            <StackPanel Margin="10, 10, 0, 0" HorizontalAlignment="Right">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Key"></TextBlock>
                <TextBox x:Name="txtKey" Width="300"></TextBox>
                <Button Content="Save" Click="ButtonBase_OnClick"></Button>
            </StackPanel>
            
        </StackPanel>
        
        <Grid Grid.Row="1" Background="Red">
            
            <CaptureElement Name="PreviewControl" Stretch="Uniform"/>
            <Button Content="Capure" Click="Capture_OnClick" VerticalAlignment="Top" HorizontalAlignment="Left" Width="50" Height="50" Margin="30"></Button>
        </Grid>
        
        <ProgressRing IsActive="False" Width="50" Height="50" x:Name="ProgressRing" VerticalAlignment="Center" HorizontalAlignment="Center"></ProgressRing>
        
        <MediaElement Opacity="0" Width="1" Height="1" x:Name="SpeechElement"></MediaElement>
        
    </Grid>
</Page>
